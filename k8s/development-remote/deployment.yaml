apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-monnom-MonApplication-backend-deployment
  namespace: dev-monnom-MonApplication-backend
  labels:
    app: dev-monnom-MonApplication-backend
spec:
  selector:
    matchLabels:
      app: dev-monnom-MonApplication-backend
  template:
    metadata:
      labels:
        app: dev-monnom-MonApplication-backend
    spec:
      containers:
        - name: dev-monnom-MonApplication-backend
          image: corentin35/monnom-MonApplication-backend:develop-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3333
          env:
            - name: PORT
              value: '3333'
          resources:
            requests:
              cpu: '300m'
              memory: '400Mi'
            limits:
              cpu: '600m'
              memory: '800Mi'
      imagePullSecrets:
        - name: regcred
