apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prod-monnom-MonApplication-backend-cert
  namespace: prod-monnom-MonApplication-backend
spec:
  secretName: prod-monnom-MonApplication-backend-tls
  dnsNames:
    - monsousdomain2.api.flapi.org
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
