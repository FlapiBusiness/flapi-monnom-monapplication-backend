apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-monnom-MonApplication-backend-ingress
  namespace: prod-monnom-MonApplication-backend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - monsousdomain2.api.flapi.org
      secretName: prod-monnom-MonApplication-backend-tls
  rules:
    - host: monsousdomain2.api.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-monnom-MonApplication-backend-service
                port:
                  number: 3333
