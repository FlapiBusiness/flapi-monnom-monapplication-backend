apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-monnom-MonApplication-backend-deployment
  namespace: prod-monnom-MonApplication-backend
  labels:
    app: prod-monnom-MonApplication-backend
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: prod-monnom-MonApplication-backend
  template:
    metadata:
      labels:
        app: prod-monnom-MonApplication-backend
    spec:
      containers:
        - name: prod-monnom-MonApplication-backend
          image: corentin35/monnom-MonApplication-backend:${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3333
          env:
            - name: PORT
              value: '3333'
          resources:
            requests:
              cpu: '300m'
              memory: '400Mi'
            limits:
              cpu: '600m'
              memory: '800Mi'
      imagePullSecrets:
        - name: regcred
