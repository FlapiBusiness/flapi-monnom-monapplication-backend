apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-monnom-MonApplication-backend-ingress
  namespace: staging-monnom-MonApplication-backend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.monsousdomain2.api.flapi.org
      secretName: staging-monnom-MonApplication-backend-tls
  rules:
    - host: staging.monsousdomain2.api.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-monnom-MonApplication-backend-service
                port:
                  number: 3333
