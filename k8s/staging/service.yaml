apiVersion: v1
kind: Service
metadata:
  name: staging-monnom-MonApplication-backend-service
  namespace: staging-monnom-MonApplication-backend
spec:
  ports:
    - port: 3333
      targetPort: 3333
  selector:
    app: staging-monnom-MonApplication-backend
